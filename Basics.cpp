{{{cpp
Declarations  : return_type name (arg);

	return_type name;
				


Definition FULL:  return_type name (arg/Parameter) {body};

	return_type name = value;


Function call:
		
		function_name (); 

Function declaration: aka function prototype

	Parameters should be seperated by a comma.


Varaiables declared in a code unit are destroyed after the program exits the code. 

Using rand without a seed will output the same numbers consistenlty

